# Emphasis 模板项目集合

这个仓库包含了多个前端项目模板，用于快速初始化不同类型的项目。

## 自动化模板信息生成

本项目包含一个自动化脚本，用于在代码提交时自动生成 `template.json` 文件，该文件包含了所有模板的基本信息。

### 手动生成模板信息

如果需要手动生成模板信息，可以运行：

```bash
make generate-templates
```

### 查看所有可用命令

```bash
make help
```

